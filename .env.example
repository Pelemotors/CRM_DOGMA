# Wonder AI — local secrets (never commit)
# Copy to .env and set a long random value.

AI_API_KEY=change-me-to-a-long-random-secret
# AI_RATE_LIMIT_PER_MIN=60

# Write tools are FAIL-CLOSED by default (403).
# Only set the following to exactly 1 to allow upsert/followup/appointment/outcome:
# AI_WRITE_ENABLED=1
